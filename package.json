{
  "name": "odoo-hackathon",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix frontend install --legacy-peer-deps && npm --prefix frontend run build && npm --prefix backend install --omit=dev",
    "start": "npm --prefix backend run start"
  },
  "engines": {
    "node": "20.19.0"
  }
}
